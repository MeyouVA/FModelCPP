#include "Aes.h"

#include <cstring>
#include <stdexcept>

namespace CUE4Parse::Encryption::Aes
{
    namespace
    {
        // FIPS-197 inverse S-box.
        constexpr uint8_t InvSBox[256] = {
            
        };

        // Forward S-box — needed by the key schedule (SubWord), which is a *forward* operation even when
        // only decrypting.
        constexpr uint8_t SBox[256] = {
            
        };

        // AES-256: Nk = 8 key words, Nr = 14 rounds, so 15 round keys of 16 bytes.
        constexpr int Nk = 8;
        constexpr int Nr = 14;
        constexpr int ExpandedKeyBytes = (Nr + 1) * 16;

        // Multiplication in GF(2^8) with the AES reduction polynomial 0x11b.
        inline uint8_t GMul(uint8_t a, uint8_t b)
        {
            uint8_t result = 0;
            while (b != 0)
            {
                if (b & 1) result ^= a;
                const bool high = (a & 0x80) != 0;
                a = static_cast<uint8_t>(a << 1);
                if (high) a ^= 0x1b;
                b = static_cast<uint8_t>(b >> 1);
            }
            return result;
        }

        struct RoundKeys
        {
            uint8_t Bytes[ExpandedKeyBytes];
        };

        // FIPS-197 §5.2 KeyExpansion, specialised to Nk = 8.
        RoundKeys ExpandKey(const std::vector<uint8_t>& key)
        {
            if (key.size() != 32)
                throw std::invalid_argument("Aes Key must be 32 bytes long");

            RoundKeys rk{};
            std::memcpy(rk.Bytes, key.data(), 32);

            uint8_t rcon = 1;
            for (int i = Nk; i < (Nr + 1) * 4; ++i)
            {
                uint8_t t[4] = {
                    rk.Bytes[(i - 1) * 4 + 0], rk.Bytes[(i - 1) * 4 + 1],
                    rk.Bytes[(i - 1) * 4 + 2], rk.Bytes[(i - 1) * 4 + 3]
                };

                if (i % Nk == 0)
                {
                    // RotWord + SubWord + Rcon
                    const uint8_t tmp = t[0];
                    t[0] = static_cast<uint8_t>(SBox[t[1]] ^ rcon);
                    t[1] = SBox[t[2]];
                    t[2] = SBox[t[3]];
                    t[3] = SBox[tmp];
                    rcon = GMul(rcon, 2); // xtime; stays exact for all seven values AES-256 uses
                }
                else if (i % Nk == 4)
                {
                    // The extra SubWord that only 256-bit keys perform.
                    t[0] = SBox[t[0]]; t[1] = SBox[t[1]]; t[2] = SBox[t[2]]; t[3] = SBox[t[3]];
                }

                for (int j = 0; j < 4; ++j)
                    rk.Bytes[i * 4 + j] = static_cast<uint8_t>(rk.Bytes[(i - Nk) * 4 + j] ^ t[j]);
            }
            return rk;
        }

        inline void AddRoundKey(uint8_t* state, const RoundKeys& rk, int round)
        {
            const uint8_t* k = rk.Bytes + round * 16;
            for (int i = 0; i < 16; ++i) state[i] ^= k[i];
        }

        // Row r rotates right by r. State index is r + 4*c (FIPS-197 column-major input mapping).
        inline void InvShiftRows(uint8_t* state)
        {
            uint8_t tmp[16];
            for (int c = 0; c < 4; ++c)
                for (int r = 0; r < 4; ++r)
                    tmp[r + 4 * ((c + r) % 4)] = state[r + 4 * c];
            std::memcpy(state, tmp, 16);
        }

        inline void InvSubBytes(uint8_t* state)
        {
            for (int i = 0; i < 16; ++i) state[i] = InvSBox[state[i]];
        }

        inline void InvMixColumns(uint8_t* state)
        {
            for (int c = 0; c < 4; ++c)
            {
                uint8_t* p = state + 4 * c;
                const uint8_t a0 = p[0], a1 = p[1], a2 = p[2], a3 = p[3];
                p[0] = static_cast<uint8_t>(GMul(a0, 14) ^ GMul(a1, 11) ^ GMul(a2, 13) ^ GMul(a3, 9));
                p[1] = static_cast<uint8_t>(GMul(a0, 9) ^ GMul(a1, 14) ^ GMul(a2, 11) ^ GMul(a3, 13));
                p[2] = static_cast<uint8_t>(GMul(a0, 13) ^ GMul(a1, 9) ^ GMul(a2, 14) ^ GMul(a3, 11));
                p[3] = static_cast<uint8_t>(GMul(a0, 11) ^ GMul(a1, 13) ^ GMul(a2, 9) ^ GMul(a3, 14));
            }
        }

        // FIPS-197 §5.3 InvCipher (the straightforward, not the equivalent, inverse).
        void DecryptBlock(uint8_t* block, const RoundKeys& rk)
        {
            AddRoundKey(block, rk, Nr);
            for (int round = Nr - 1; round >= 1; --round)
            {
                InvShiftRows(block);
                InvSubBytes(block);
                AddRoundKey(block, rk, round);
                InvMixColumns(block);
            }
            InvShiftRows(block);
            InvSubBytes(block);
            AddRoundKey(block, rk, 0);
        }
    }

    std::vector<uint8_t> Aes::Decrypt(const std::vector<uint8_t>& encrypted, const FAesKey& key)
    {
        return Decrypt(encrypted, 0, static_cast<int>(encrypted.size()), key);
    }

    std::vector<uint8_t> Aes::Decrypt(const std::vector<uint8_t>& encrypted, int beginOffset, int count, const FAesKey& key)
    {
        if (beginOffset < 0 || count < 0 || static_cast<size_t>(beginOffset) + static_cast<size_t>(count) > encrypted.size())
            throw std::invalid_argument("Aes::Decrypt range is outside the buffer");

        std::vector<uint8_t> result(encrypted.begin() + beginOffset, encrypted.begin() + beginOffset + count);
        DecryptInPlace(result.data(), count, key);
        return result;
    }

    void Aes::DecryptInPlace(uint8_t* data, int count, const FAesKey& key)
    {
        // PaddingMode.None means .NET rejects a partial final block rather than padding it out.
        if (count % ALIGN != 0)
            throw std::invalid_argument("Aes::Decrypt input length must be a multiple of 16");

        const RoundKeys rk = ExpandKey(key.Key);
        for (int offset = 0; offset < count; offset += ALIGN)
            DecryptBlock(data + offset, rk);
    }
}
